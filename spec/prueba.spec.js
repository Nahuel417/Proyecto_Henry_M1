// describe("demo", function () {
//   it("Este test debe pasar siempre", function () {
//     expect(4 + 2).toBe(6);
//   });
// });

const {Activity, Repository} = require('../scripts/index')

describe('La clase toDoList', () => {

  it('Activity debe ser una clase', function () {
    expect(typeof toDoList.prototype.constructor).toBe('function')
  });

  it('Repository debe ser una clase', function () {
    expect(typeof toDoList.prototype.constructor).toBe('function')
  });

  it('Repository debe tener implementado el metodo getAllActivities()', function () {
    expect(lista.toDoList)
  })

  it('Repository debe tener implementado el metodo createActivity()', function () {
    expect(lista.toDoList)
  })

  it('Repository debe tener implementado el metodo deleteActivity()', function () {
    expect(lista.toDoList)
  })

  it('El metodo getAllActivities() debe retonar un array', function () {
    expect(lista.toDoList)
  })

  it('El metodo createActivity() debe agregar un nuevo elemento  ', function () {
    expect(lista.toDoList)
  })

  it('El metodo deleteActivity() debe retonar un array', function () {
    expect(lista.toDoList)
  })

})



